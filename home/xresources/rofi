! [Rofi][1] Configuration
!
! "To get a template config file run: rofi -dump-xresources".
!    -- rofi(1), "CONFIGURATION" section
!
! [1]: https://github.com/DaveDavenport/rofi

rofi.dpi: 135

! TODO: modify the "$TERMINAL" environment variable?
rofi.terminal: xterm

! Use my `jellyjam.rasi` theme file.  This should cause Rofi (1.4 or later) to ignore most
! of the settings below.  TODO: remove the settings that are ignored now and replace any
! that aren't ignored but deprecated.
rofi.theme: jellyjam

! Reduce the height of the window based on the number of items.  See
! <https://github.com/DaveDavenport/rofi/releases/tag/1.3.0>.  Off by default (i.e.
! `fixed-num-lines` is true).
rofi.fixed-num-lines: false
! The maximum number of item rows that will be shown (default: 15).
rofi.lines: 13

! Width of the window in characters (the minus switches from specifying a percentage of
! the screen's width to specifying a count of characters; default: 50).
rofi.width: -80

! Vertically center the separator line (the offset to vertically center the window when
! all 13 item rows are shown would be -122).
rofi.yoffset: -8

! Specify a font.  The font used affects the startup time of Rofi considerably.  Dina
! gives the best result of the fonts below which seems logical as it's the only bitmap
! font.  Switching between vector fonts still has a considerable effect, though.  Ubuntu
! Mono gives the worst results.
rofi.font: Dina 5.81
! rofi.font: Ubuntu Mono 7
! rofi.font: Source Code Pro Medium 6
! rofi.font: Source Code Pro 6
! rofi.font: Mono 7

! Some notes.
!
! 1. I should explain where the value of 5.81 comes from.  It corresponds to a font size
!    of 10 pixels in xfontsel(1) but I'm not sure why (one factor is `rofi.dpi`, though).
!    It's really just the result of trial and error.  Values in the closed interval [5.76,
!    5.86] give me my desired font size of 10 pixels without any weird scaling issues for
!    either the normal or the italic variant of Dina.  (Yes, the size of the italic
!    variant changed at different thresholds than the size of the regular one.)  5.81 is
!    the mean of the interval [5.76, 5,86].  I did my testing with Rofi 1.3.1.
! 2. I'm not sure where the format fonts need to be specified in is documented.  Here are
!    two formats that I used before for other programs, but that are wrong for Rofi:
!       rofi.font: -*-dina-medium-r-*-*-10-*-*-*-*-*-*-*
!       rofi.font: Dina-5:style=regular
!    TODO: attain some clarity about the different ways fonts are specified in various
!    programs' configuration files and add some enlightening comment about the format Rofi
!    expects here.

! Border width (default: 1)
rofi.bw: 1
! Inner margin of the window (default: 5)
rofi.padding: 4
! Spacing between rows (default: 2)
rofi.line-margin: 2
! Padding within rows (default: 1)
rofi.line-padding: 2
! Draw a solid line between the text entry and the items (default: dash).
rofi.separator-style: solid
! Width of the scrollbar in pixels (default: 8)
rofi.scrollbar-width: 4

! Color theme.  See rofi(1), <https://davedavenport.github.io/rofi/p11-Generator.html>,
! and <https://davedavenport.github.io/rofi/p05-Themes.html>.
!                  bg,      border,  separator
rofi.color-window: #000000, #c0c0c0, #8787af
!                  bg,      fg,      alt bg,  hi bg,   hi fg
rofi.color-normal: #000000, #dfdfdf, #060606, #121212, #dfdfdf
rofi.color-urgent: #000000, #d75f5f, #060606, #121212, #d75f5f
rofi.color-active: #000000, #8787af, #060606, #121212, #8787af

! Key binding modifications.  Behave more like [Readline][2].
rofi.kb-clear-line:
rofi.kb-remove-word-back: Control+w

! [2]: https://en.wikipedia.org/wiki/GNU_Readline

! vim: ft=xdefaults tw=90 sts=-1 sw=3 et cms=!\ %s
