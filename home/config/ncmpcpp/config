# Configuration file for ncmpcpp.  See ncmpcpp(1).  There also should be an example
# configuration file with the default values for all settings at
# `/usr/share/doc/ncmpcpp/config`.

# XXX: do NOT gf this!
visualizer_fifo_path = /tmp/mpd.fifo
# The name "vis" is used in `home/config/mpd/mpd.conf` for the FIFO output.
visualizer_output_name = vis

visualizer_type = wave
visualizer_color = 41, 83, 119, 155, 185, 215, 209, 203, 197, 161
# visualizer_look = *0
# visualizer_look = *o
# visualizer_look = *|
# visualizer_look = *:
# visualizer_look = *#
visualizer_look = */

song_list_format = {%a - }{%t}|{$8%f$9}$R{$(244)%y$9}
song_status_format = {{$b%a$/b{ - %b{ (%y)}} - }{$b%t$/b}}|{%f}
alternative_header_first_line_format = $1$aqqu$/a$9 {%t}|{%f} $1$atqq$/a$9

current_item_prefix = $(blue)$r
selected_item_prefix = $2

song_columns_list_format = (67)[white]{t|f:Title} (33)[blue]{a} (4f)[244]{yE}

# progressbar_look = ─>
# progressbar_look = >>
# progressbar_look = //
progressbar_look = ...

data_fetching_delay = no

# header_visibility = no
# statusbar_visibility = no
titles_visibility = no

display_volume_level = no

lines_scrolled = 1
mouse_list_scroll_whole_page = no

empty_tag_color = blue
header_window_color = blue
main_window_color = default
# progressbar_color = black
progressbar_color = 236:b
progressbar_elapsed_color = default:b
statusbar_color = blue
statusbar_time_color = default
player_state_color = default
window_border_color = blue
